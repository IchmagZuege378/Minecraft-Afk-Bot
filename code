const mineflayer = require('mineflayer');

const bot = mineflayer.createBot({
  host: 'play.badiplays.de',
  port: 25565,
  username: 'IchMagZuege378'
});

// Der Bot bleibt nur verbunden – keine Aktionen
